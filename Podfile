# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Login App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Login App
    pod 'Alamofire'
    pod 'SwiftyJSON'

  target 'Login AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Login AppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
